<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ : le système dessine lui-même l'écran de lancement et ignore
         android:windowBackground. On lui passe donc le fond et l'icône via les
         attributs windowSplashScreen*. L'icône est une image de 288dp dont le
         contenu tient dans le cercle de 192dp imposé par le masque système. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/launch_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@color/launch_background</item>
    </style>
</resources>
