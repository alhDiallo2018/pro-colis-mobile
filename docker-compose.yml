services:
  procolis-web:
    build:
      context: .
      args:
        API_BASE_URL: ${API_BASE_URL:-/api/v1}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "${APP_PORT:-8081}:80"
    restart: unless-stopped
